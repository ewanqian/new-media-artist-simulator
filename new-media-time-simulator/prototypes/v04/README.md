# V0.4 Attention Loop Prototype

Live preview source is intentionally kept dependency-free for fast playtesting. The published `gh-pages/v04/` version contains `index.html`, `styles.css`, and `app.js`.

The prototype tests a text-first MUD-like interaction model with attention as the primary resource, persistent project history, archive callbacks, failure reuse, and a minimal icon/UI system.
